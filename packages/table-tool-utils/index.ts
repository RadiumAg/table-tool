import {
  RootSchema,
  ValidateCallback,
  ValidateRule,
  ValidatorFunction,
  getSchema,
  isObject,
} from './src';

export type { ValidateCallback, RootSchema, ValidateRule, ValidatorFunction };
export { isObject, getSchema };
